# babylon.ml

## Quick Start

```shell
npm run init

# In separate terminals:
npm run watch
npm run serve
```

## JavaScript output

The web app files will be placed in `_build/default/src/output/*`.
